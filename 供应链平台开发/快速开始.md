# 供应链平台 - 快速开始指南

## 📁 项目结构

```
供应链平台开发/
├── backend/              # 后端项目
│   ├── config/          # 配置文件
│   ├── controllers/     # 控制器
│   ├── models/          # 数据模型
│   ├── routes/          # 路由
│   ├── middleware/      # 中间件
│   ├── utils/           # 工具函数
│   ├── app.js          # 应用入口
│   ├── package.json    # 依赖配置
│   └── .env           # 环境变量
├── frontend/           # 前端项目
│   ├── src/
│   │   ├── api/       # API接口
│   │   ├── assets/    # 静态资源
│   │   ├── components/# 组件
│   │   ├── layouts/   # 布局
│   │   ├── router/    # 路由
│   │   ├── stores/    # 状态管理
│   │   ├── utils/     # 工具函数
│   │   ├── views/     # 页面
│   │   ├── App.vue
│   │   └── main.js
│   ├── index.html
│   ├── package.json
│   └── vite.config.js
├── database/          # 数据库脚本
│   └── init.sql      # 初始化脚本
└── README.md
```

## 🚀 快速开始

### 第一步：安装依赖

#### 后端
```bash
cd backend
npm install
```

#### 前端
```bash
cd frontend
npm install
```

### 第二步：配置数据库

1. **创建数据库**

```bash
mysql -u root -p
```

2. **执行初始化脚本**

```bash
mysql -u root -p < database/init.sql
```

或使用 MySQL 客户端打开 `database/init.sql` 文件执行。

3. **修改数据库配置**

编辑 `backend/.env` 文件：

```env
# 数据库配置
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password  # 修改为你的密码
DB_NAME=supply_chain
```

### 第三步：启动项目

#### 启动后端
```bash
cd backend
npm run dev
```

后端将运行在：http://localhost:3000

#### 启动前端
```bash
cd frontend
npm run dev
```

前端将运行在：http://localhost:5173

### 第四步：访问系统

打开浏览器访问：http://localhost:5173

## 👤 测试账号

### 供应商账号
- 用户名：supplier001
- 密码：123456

### 管理员账号
需要手动在数据库中创建：

```sql
-- 使用bcrypt加密密码 'admin123'
INSERT INTO users (username, email, phone, password, role, status) VALUES
('admin', 'admin@example.com', '13800000000', '$2a$10$XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX', 'admin', 1);
```

## 📝 开发说明

### 后端API接口

所有API接口都在 `/api` 路径下：

- `/api/auth/*` - 认证相关
- `/api/suppliers/*` - 供应商相关
- `/api/products/*` - 产品相关
- `/api/tasks/*` - 任务相关
- `/api/admin/*` - 管理后台相关

### 前端页面路由

- `/login` - 登录页
- `/register` - 注册页
- `/supplier/*` - 供应商端
- `/admin/*` - 管理后台

### 环境变量说明

#### 后端 (.env)
- `PORT` - 服务器端口
- `NODE_ENV` - 运行环境
- `DB_HOST` - 数据库主机
- `DB_USER` - 数据库用户
- `DB_PASSWORD` - 数据库密码
- `DB_NAME` - 数据库名称
- `JWT_SECRET` - JWT密钥
- `UPLOAD_PATH` - 文件上传路径

## 🔧 常见问题

### Q1: 数据库连接失败

**A:** 检查 `.env` 文件中的数据库配置是否正确。

### Q2: npm install 失败

**A:** 尝试使用淘宝镜像：
```bash
npm install --registry=https://registry.npmmirror.com
```

### Q3: 前端请求跨域

**A:** 已在 `vite.config.js` 中配置代理，确保后端服务已启动。

### Q4: 端口被占用

**A:** 修改 `.env` 或 `vite.config.js` 中的端口配置。

## 📚 相关文档

- [供应链平台开发方案.md](./供应链平台开发方案.md) - 详细开发方案
- [开发需求文档.md](../官网/开发需求文档.md) - 完整需求文档

## 🤝 开发规范

### Git 提交规范

```
feat: 新功能
fix: 修复bug
docs: 文档更新
style: 代码格式调整
refactor: 重构
test: 测试
chore: 构建/工具变动
```

### 代码规范

- 后端：使用 ESLint + Prettier
- 前端：使用 ESLint + Prettier

## 📞 技术支持

如遇到问题，请查看：
1. 控制台错误信息
2. 浏览器开发者工具 Network 面板
3. 数据库连接状态

---

**开始开发吧！** 🚀
